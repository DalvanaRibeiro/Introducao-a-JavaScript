let numero = 5; // Número cuja tabuada será calculada
let multiplicador = 1;

while (multiplicador <= 10) {
  // Imprime o resultado da multiplicação
  console.log(numero + " x " + multiplicador + " = " + (numero * multiplicador));
  multiplicador++; // Incrementa o multiplicador para o próximo cálculo
}
